#primeiro, instalar o coverage:
pip install -r requirements.txt

#para executar todos os testes:
python -m unittest discover tests -v

#para executar com cobertura:
coverage run -m unittest discover tests
coverage report
coverage html

#para executar um teste em específico:
python -m unittest tests.test_unidade.TestCalculadora.test_soma -v

#OBS: Os testes foram realizados em um virtual environment (venv)
#para criar uma venv:
python -m venv nomeDaVenv

#para acessa-la:
.\nomeDaVenv\Scripts\activate

#para sair dela:
deactivate